meta {
  name: Registry List Agents
  type: http
  seq: 5
}

get {
  url: {{registryBaseUrl}}/agents?top=20&skip=0
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}
