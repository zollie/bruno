meta {
  name: Get Agent
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/agents/tenants/{{tenantId}}/{{agentId}}
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}
