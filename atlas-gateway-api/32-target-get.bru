meta {
  name: Target Get
  type: http
  seq: 32
}

get {
  url: {{baseUrl}}/gateway/tenants/{{tenantId}}/admin/targets/{{targetId}}
  body: none
}

headers {
  Authorization: Bearer {{adminToken}}
}

assert {
  res.status: eq 200
}
