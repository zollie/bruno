meta {
  name: Delete Flow
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/flows/tenants/{{tenantId}}/flows/{{flowId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 204
}
