meta {
  name: Get Ingestion
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/indexing/tenants/{{tenantId}}/ingestions/{{jobId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
}
